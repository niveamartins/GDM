import { ReactNode } from 'react';
export interface Props {
    children: ReactNode;
    constraints: MediaStreamConstraints;
}
declare function UserMediaProvider({ children, constraints }: Props): JSX.Element;
export default UserMediaProvider;
